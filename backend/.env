# Database Configuration
DB_NAME=sakila
DB_USER=root
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=3306   # Default MySQL port, change if different

# Server Configuration
PORT=4000

# Environment Mode
NODE_ENV=development

# Optional: JWT Secret (for authentication, if needed)
JWT_SECRET=your_jwt_secret_here

# Optional: Logging Level (if you configure logging)
LOG_LEVEL=info
